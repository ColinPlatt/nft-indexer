{
  "name": "alto",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-canto": "npx graph create --node http://localhost:8020 canto/alto  --access-token $ACCESS_TOKEN",
    "deploy-canto": "npx graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 canto/alto --access-token $ACCESS_TOKEN",
    "remove-canto": "npx graph remove --node http://localhost:8020 canto/alto  --access-token $ACCESS_TOKEN",
    "test": "graph test"
  },
  "dependencies": {
    "@amxx/graphprotocol-utils": "^1.1.0",
    "@graphprotocol/graph-cli": "0.34.0",
    "@graphprotocol/graph-ts": "0.28.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
